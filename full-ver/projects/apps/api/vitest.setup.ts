/**
 * @what Vitest global setup file
 * @why Configure test environment and load custom matchers
 */

// Load custom Result matchers globally
import './__tests__/matchers/result-matchers.js';

// Global test configuration can be added here
// Example: setup test database, global mocks, etc.
