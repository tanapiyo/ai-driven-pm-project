/**
 * @layer features
 * @segment dashboard
 * @what Dashboard Feature Public API
 *
 * features は entities, shared のみを import できる
 * 外部からは index.ts 経由でのみアクセスされる
 */

export { DashboardHome } from './ui';
