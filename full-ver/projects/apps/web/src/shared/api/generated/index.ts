/**
 * @layer shared
 * @segment api
 * @what OpenAPI から自動生成される型/クライアント
 *
 * このディレクトリの内容は `pnpm openapi:generate` で生成される
 * 手動編集禁止
 */

// 生成物のプレースホルダー
// 実際の生成物は orval/openapi-typescript で生成される

export interface GeneratedPlaceholder {
  _notice: 'Run `pnpm openapi:generate` to generate API types';
}
