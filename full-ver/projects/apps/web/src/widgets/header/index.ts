/**
 * @layer widgets
 * @segment header
 * @what ヘッダーウィジェット - public API
 *
 * widgets は features, entities, shared のみを import できる
 * 外部からは index.ts 経由でのみアクセスされる
 */
export { Header } from './ui/Header';
