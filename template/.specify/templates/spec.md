# Spec: [機能名]

<!-- Spec は「何を作るか」を定義するドキュメント。「どう作るか」は Plan に書く。
     ファイル名の規則: .specify/specs/<id>/spec.md
     <id> は Issue 番号か機能の識別子（例: 042-expense-approval）。
     このファイルが完成し、AC が人間によって確定されてから実装を開始する。 -->

## 対応 Issue

Closes #

## 目的

<!-- 「誰の何を解決するか」を 2〜3 文で書く。PRD の該当箇所を具体化したもの。
     書き方: なぜこの機能が必要か → 何ができるようになるか。
     例: 経費申請者がリモートから申請を提出できない（Issue #42）。
         この機能で Slack から申請フォームを送信できるようになる。
         総務担当は Slack 上で承認・差し戻しを行えるようになる。
     NG: 「申請機能を追加する」← 目的ではなく作業内容 -->

## スコープ

<!-- In Scope: この機能で実装するもの（完了の判定対象）。
     Out of Scope: あえて含めないもの（後で「なぜ作らなかった？」を防ぐ）。
     例:
     In Scope:
     - Slack のスラッシュコマンド /expense で申請モーダルを開く
     - 申請内容（金額・用途・日付）の入力と送信
     Out of Scope:
     - 領収書画像の添付（次フェーズ）
     - 複数承認者の連鎖承認（シングル承認のみ） -->

**In Scope:**
-

**Out of Scope:**
-

---

## Functional Requirements (FR)

<!-- 「何ができるか」を箇条書きで書く。実装方法は書かない。
     書き方: 主語（誰が）+ 動詞（何を）+ 目的語（何に対して）。
     例:
     1. 申請者は /expense コマンドで申請モーダルを開くことができる
     2. 申請者は金額・用途・申請日を入力して送信できる
     3. 承認者は Slack DM で承認依頼を受け取り、承認 or 差し戻しを選択できる
     NG: 「Slack API を使って実装する」← 実装方法 -->

1.
2.
3.

---

## Non-Functional Requirements (NFR)

<!-- 動作条件・品質要件を数値で書く。「高速に」「安全に」はNG。
     書かなくてよい項目は「特になし」と書く（空欄は「未検討」に見える）。
     例:
     - Performance: Slack コマンド送信から 2 秒以内にモーダルが開く
     - Security: 申請データは申請者・承認者・総務のみ参照可能
     - Availability: 特になし（Slack 本体の可用性に依存） -->

- **Performance**:
- **Security**:
- **Availability**:

---

## Acceptance Criteria (AC)

<!-- AC = 実装の完了判定基準。ここを人間が確定してから実装に入る。
     書き方: Given（前提状態）/ When（操作）/ Then（期待結果）。
     - Given: システムやユーザーの初期状態
     - When: ユーザーが行う操作
     - Then: システムが返す結果（具体的な画面表示・データ・動作）
     例:
     - AC1: Given 申請者が Slack にログイン済み
             When /expense と入力して送信する
             Then 申請モーダル（金額・用途・日付フィールド）が表示される
     - AC2: Given 申請モーダルが開いている
             When 金額欄を空欄のまま送信する
             Then 「金額を入力してください」のバリデーションエラーが表示される

     チェック: AC が全部通れば「この機能は完成した」と言えるか？
               言えなければ AC が足りない。 -->

- [ ] **AC1**: Given ... When ... Then ...
- [ ] **AC2**: Given ... When ... Then ...
- [ ] **AC3**: Given ... When ... Then ...

---

## 関連ドキュメント

- Plan: `./plan.md`
- ADR: `docs/02_architecture/adr/NNNN-*.md`（アーキテクチャ変更がある場合）
